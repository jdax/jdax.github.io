---
layout: layout
title: "Index"
pageable: true
per_page: 10
---

<div class="page-header">
  <h1>Nikki Murray: <small>Book reviews, coding thoughts, and feminist rants. In no particular order.</small></h1>
</div>
<div class="container-fluid">
<% if paginate && num_pages > 1 %>
  <p>Page <%= page_number %> of <%= num_pages %></p>

  <% if prev_page %>
    <p><%= link_to 'Previous page', prev_page %></p>
  <% end %>
<% end %>


<% page_articles.each_with_index do |article, i| %>
      <div class="panel panel-default">
 <div class="panel-heading"> <h3 class="panel-title"><%= link_to article.title, article %> <span><%= article.date.strftime('%b %e') %></span></h3></div>
  <!-- use article.summary(250) if you have Nokogiri available to show just
       the first 250 characters -->
  <div class="panel-body"><%= article.body %></div>
      </div>
<% end %>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Next page', next_page %></p>
  <% end %>
<% end %>
</div>